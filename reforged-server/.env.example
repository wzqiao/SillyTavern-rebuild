# Copy values into your process manager, shell, or hosting environment.
# Do not commit a real .env file.

REFORGED_PORT=8787
REFORGED_GENERATION_MODE=proxy
REFORGED_ALLOWED_ORIGIN=https://<your-subdomain.example.com>
REFORGED_TOKEN=<generate-with-openssl-rand-base64-32>
